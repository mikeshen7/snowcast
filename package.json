{
  "name": "snowcast",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "dev:proxy": "concurrently \"npm --prefix backend run dev\" \"cross-env REACT_APP_BACKEND_URL= npm --prefix frontend start\"",
    "build": "npm --prefix frontend run build",
    "start": "node backend/server.js",
    "start:full": "npm run build && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "tz-lookup": "^6.1.25"
  }
}
