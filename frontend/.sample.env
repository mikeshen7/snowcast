# Frontend configuration
# If the API is on the same origin (proxy in dev), leave REACT_APP_API_BASE_URL empty.
REACT_APP_BACKEND_URL=

# Optional: API key for weather-backend (sent as x-api-key on all requests).
REACT_APP_BACKEND_API_KEY=
